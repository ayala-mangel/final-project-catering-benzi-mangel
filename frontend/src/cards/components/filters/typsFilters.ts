export type EVENT_OPTIONS = {
  wedding: [
    open: {
      salad: [chomoos: string, batata: string, chazil: string, chruv: string];
      brad: [lachmaniot: string, frena: string, lafa: string];
    },
    first: {
      fish: [salomon: string, moosht: string, denis: string];
      liver: string;
    },
    main: {
      meat: [asado: string];
      pargit: string;
      chicken: string;
    },
    extra: {
      plate: [batata: string, antipasti: string];
      table: [rice: string];
    },
    last: {
      bar: [petipur: string, fruit: string, cake: string];
      ice_cream: [vanil: string, chocalate: string];
      plate: string;
    },
    drink: {
      cola: string;
      water: string;
      orange: string;
      wine: [red: string, white: string, wiski: string];
    }
  ];
  small: [];
};
